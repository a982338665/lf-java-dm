<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间  会映射到 接口-->
<mapper namespace="dameng.dao.ProductCategoryMapper">
    <!--parameterType 参数类型 -->
    <!--resultType 返回结果集类型 -->
    <!--查询所有数据-->
    <select id="selectAll" resultType="dameng.pojo.ProductCategory">
      select * from PRODUCTION2.PRODUCT_CATEGORY;
    </select>
    <!--查询总条数-->
    <select id="selectCount" resultType="int">
      select count(*) from PRODUCTION2.PRODUCT_CATEGORY;
    </select>
    <!--根据 ID 查询指定信息-->
    <!--有参数   有返回值-->
    <select id="selectProductCategoryById" parameterType="java.lang.Integer"
            resultType="dameng.pojo.ProductCategory">
      select * from PRODUCTION2.PRODUCT_CATEGORY WHERE PRODUCT_CATEGORYID = #{PRODUCT_CATEGORYID};
    </select>
    <!--插入信息   -->
    <insert id="insertProductCategory" parameterType="dameng.pojo.ProductCategory"  useGeneratedKeys="false">
        INSERT into PRODUCTION2.PRODUCT_CATEGORY(PRODUCT_CATEGORYID,name)VALUES(#{PRODUCT_CATEGORYID},#{NAME});
    </insert>
    <!--根据 id 删除信息-->
    <delete id="deleteById" parameterType="int">
        delete from PRODUCTION2.PRODUCT_CATEGORY where PRODUCT_CATEGORYID = #{PRODUCT_CATEGORYID};
    </delete>

    <!--根据 id 修改信息-->
    <update id="updateProductCategory" parameterType="dameng.pojo.ProductCategory">
        update PRODUCTION2.PRODUCT_CATEGORY set NAME = #{NAME}
        where PRODUCT_CATEGORYID = #{PRODUCT_CATEGORYID}
    </update>
    <update id="autoInsert">
        SET IDENTITY_INSERT #{s} ON
    </update>
</mapper>
